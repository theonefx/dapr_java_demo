server:
  port: 8080

management:
  server:
    port: 8180
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always

spring:
  application:
    name: demo-consumer